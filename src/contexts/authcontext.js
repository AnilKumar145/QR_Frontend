// This file should be removed as it's causing confusion
// The actual implementation is in AuthContext.tsx
// If you need to keep this file for backward compatibility, update it to:

import { AuthProvider, AuthContext } from './AuthContext.tsx';

export { AuthProvider, AuthContext };
